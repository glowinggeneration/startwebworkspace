# Exception Register — Startweb Workspace

Per Application Build Master Rules §16. Every rule not currently met is
recorded here with an owner and a deadline. Exceptions expire — they are
not permanent permission to skip the standard. Seeded during the Phase 0
build; update it (don't just accumulate) as items close or new ones are
found.

| Rule                                                               | Reason                                                                                                                                                                                                                                                                                             | Risk                                                                                                                                                                                                        | Temporary control                                                                                                                                                       | Owner | Deadline | Approval |
| ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | -------- | -------- |
| §6.4 MFA required for privileged accounts                          | Supabase Auth is configured for email/password only; no MFA enrollment flow exists yet.                                                                                                                                                                                                            | An `owner`/`admin` account takeover requires only that one password.                                                                                                                                        | None beyond Supabase's own session security.                                                                                                                            | TBD   | TBD      | TBD      |
| §6.5 Rate limiting is email-keyed only, not IP-keyed               | `check-auth-rate-limit.ts` gates `/auth` and `/register` server-side (wired in Phase 4), but keys the bucket on email alone — no deployment host is chosen yet, and extracting a real client IP is host-specific.                                                                                  | An attacker rotating through many email addresses from one IP isn't throttled by IP; export/OTP rate limiting is still unwired since no export feature or OTP flow exists yet.                              | Supabase Auth's own default protections; the per-email limit still stops credential-stuffing against one known account.                                                 | TBD   | TBD      | TBD      |
| §7.1 / §11.2 Audit logging is not comprehensive yet                | `audit_log` table + `log_audit_event()` RPC are live (Phase 4) and called on invitation created/accepted, but no other sensitive action (data export, invoice void, direct role edits) calls it yet.                                                                                               | Those other actions aren't traceable after the fact until each is wired individually.                                                                                                                       | Database-level history (created_at/updated_at, migrations) covers schema-level changes; row-level user actions outside invitations aren't audited yet.                  | TBD   | TBD      | TBD      |
| §6.3 Client-role RLS scoping is unverified against a live database | Phase 4 scopes the `client` role to their own account's accounts/contacts/projects/project_phases/quotes/invoices/payments and denies everything else, but this environment has no provisioned Supabase project to actually run the policies against — reviewed by reading the SQL, not exercised. | An RLS policy that looks correct on paper can still have a logic error only a real query surfaces (e.g. a missed table, a wrong join).                                                                      | Every client-facing policy follows the same `can_view_account()`/`is_client_role()` helper pattern rather than one-off logic, which narrows where a mistake could hide. | TBD   | TBD      | TBD      |
| §8.1 Idempotency on retryable state-changing operations            | `idempotency.server.ts` was lifted as a scaffold; the one multi-step write in Phase 0 (workspace creation + first membership row in `routes/onboarding.tsx`) is not wrapped with an idempotency key.                                                                                               | A double-click or retried request during onboarding could create two workspaces for the same user (not a duplicate _membership_, since RLS/unique constraints prevent that, but a duplicate workspace row). | The insert is a single round trip and the UI disables the submit button while pending; not a systematic guarantee.                                                      | TBD   | TBD      | TBD      |
| §8.1 Idempotency on quote/invoice creation                         | `next_document_number()` allocates numbers atomically (row-locked DB function), so two concurrent creates can never collide on the _number_ — but the create flow itself (number → insert document → insert line items) is not wrapped in a request-level idempotency key.                         | A double-click or retried request could create two separate quotes/invoices, each with its own valid number, for one intended submission.                                                                   | Create buttons disable while the mutation is pending; not a systematic guarantee against a retried network request.                                                     | TBD   | TBD      | TBD      |
| §9.\* AI feature rules                                             | No AI feature exists in this product yet.                                                                                                                                                                                                                                                          | N/A until one is built.                                                                                                                                                                                     | N/A                                                                                                                                                                     | —     | —        | —        |
| §11.1 Structured production observability                          | No logging/metrics/tracing pipeline is wired up; this is a Phase 0 scaffold with no deployment target chosen yet.                                                                                                                                                                                  | Production issues would only be diagnosable via the hosting provider's own logs, if any.                                                                                                                    | None yet.                                                                                                                                                               | TBD   | TBD      | TBD      |
| §12.1 Automated accessibility testing                              | No axe/pa11y integration exists in `bun run test` or `bun run build` yet.                                                                                                                                                                                                                          | Regressions against WCAG 2.2 AA could ship undetected between manual reviews.                                                                                                                               | Components are built from shadcn/Radix primitives (accessible by default) and manual keyboard/contrast review is expected before each phase's release.                  | TBD   | TBD      | TBD      |
| §13 CI pipeline / secret scanning                                  | No `.github/workflows` (or equivalent) exists yet — this environment has no `gh` CLI to set one up.                                                                                                                                                                                                | Regressions and accidentally committed secrets aren't caught automatically before merge.                                                                                                                    | `.gitignore` excludes `.env*`; `.env.example` documents required variables without real values.                                                                         | TBD   | TBD      | TBD      |
| Favicon / brand icon mark                                          | The supplied STARTWEB_Logo_Pack contains wordmark lockups only (on solid-color backgrounds), no square icon-only mark suitable for a favicon at 16–32px.                                                                                                                                           | The current favicon (`public/favicon.svg`) is a placeholder monogram, not an approved brand asset.                                                                                                          | Uses the brand blue (#003DFF) so it's on-brand in color even if not an approved mark.                                                                                   | TBD   | TBD      | TBD      |

## How to use this register

1. When a Master Rule can't be met before a release, add a row here instead of silently shipping the gap.
2. Fill in **Owner** and **Deadline** before merging — "TBD" is a placeholder for the Phase 0 pass, not a resting state.
3. When an item is resolved, delete the row (git history keeps the record) rather than marking it "done" in place.
4. This file should shrink over time. A growing register is a signal to stop adding scope and pay down risk instead.
