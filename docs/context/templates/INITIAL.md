# Brief: <short title>

Copy this file to `docs/context/work/<slug>.md` and fill it in before any code.

## Goal

One or two sentences. What the person using the app can do afterwards that
they cannot do now.

## Screens involved

Routes, and whether each is new or changed.

## Data involved

Tables, columns and policies. State clearly whether a migration is needed.

## Rules and edge cases

Business rules, permissions, what happens when there is no data, no
permission, or the action fails.

## Existing pieces to reuse

Hooks, shell primitives, vendored components. Check
`docs/context/ARCHITECTURE.md` and `docs/ui-components/COMPONENT_MAP.md`.

## Acceptance checks

A short list someone else can run in the preview to agree it is done.

## Out of scope

What this change deliberately does not touch.
